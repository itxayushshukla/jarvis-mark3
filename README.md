# jarvis-mark3
Single HTML file. Voice in/out via Web Speech API. Commands route to: hardcoded replies (time/date), site opener (20 sites), or GPT-4o-mini fallback. Hologram mode uses MediaPipe Hands on webcam canvas — draw/drag/stop via finger gestures. IP cam streams MJPEG snapshots. Two mic instances switch between main and holo mode.
